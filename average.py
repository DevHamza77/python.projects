import statistics

print(statistics.mean([90,100]))

# import is a function that allows us to import functions and arguments from other libraries
# mean is a function from the statistics library
# (.) tells us the mean function which is in the library "statistics"