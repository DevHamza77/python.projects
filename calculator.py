x = (input("What's x? "))
y = (input("What's y? "))
z =(round(float(x) + float(y)))
print (z)
# int stands for integer which is a whole number but can be changed to float for decimal numbers
# if the user enters a decimal number and you want it as an integer, use round (number [, ndigits])
# [, ndigits] stands for the number of decimal places to round to but is optional
 